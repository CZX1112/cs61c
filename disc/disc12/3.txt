3. 弗林的分类法

3.1 解释 SISD 并举例（如果有）。

单指令单数据； 每条指令都按顺序执行，作用于单个数据流。 例如，传统的计算机程序。

3.2 解释 SIMD 并给出示例（如果有）。

单指令多数据； 每条指令按顺序执行，作用于多个数据流。 例如，SSE 内在函数。

3.3 解释 MISD 并给出一个示例（如果有）。

多指令单数据； 多个指令同时执行，作用于单个数据流。 现代没有好的例子。

3.4 解释 MIMD 并给出一个示例（如果有）。

多指令多数据； 多个指令同时执行，作用于多个数据流。 例如，MapReduce或多线程程序。